<p></p>
<div class="message">Přihlášení proběhlo úspěšně.</div>
<!--	<div class="entry-content">
    <?php echo $content;
	if ($session->read('Auth.User.role')=='admin' || $session->read('Auth.User.role')=='editor_alfa' || $session->read('Auth.User.role')=='editor_youth'|| $session->read('Auth.User.role')=='editor_catholic') {
			echo '<p>'.$this->Html->link('Upravit stránku', '/content/edit/' .$name).'</p>';	
			} ?>
  </div>-->
